# Progress

Phase checklist (each phase ends with a docs update + adversarial review):

- [x] 1. Skeleton (2026-08-24: REST verified via curl — repo path validation + ~ expansion, task CRUD/enqueue/filter, settings defaults; tsc clean)
- [x] 1r. Phase 1 adversarial review — pass with 6 required fixes, all applied + minors (transitionTask composite, resolveChildCompletion, Host allowlist, zod config validation, error handler, auth.ts module, boot-config validation, absolute-path rule)
- [x] 2. UI shell: sidebar/header/theme, all four pages, task CRUD, slide-over, terminal drawer client (tsc clean, vite build ok, SPA served; fix-verification via curl)
- [x] 2r. Phase 2 adversarial review — pass with 5 required fixes (R1 wsRef guard, R7 slideover/terminal z-order, R12 queued exit, R13 review→done, R14 strict schemas) + 9 minors; all applied
- [x] 2b. Design tokens adopted (docs/tm-design-tokens.html → theme.css); auto permission mode default; model/effort dropdowns (config + per-task); run stats scaffolding (session_id/transcript_path/stats columns, chips UI)
- [x] 3. PTY + terminal: SessionManager + ring buffer, WS terminal (Origin+token, auth rejections verified), runs/orchestrator routes, orchestrator runNow/cancel/kill, model routing (fable→opus @85% + keyword rule), usage estimator + header pill. Verified: zsh PTY round-trip; real claude worker spawned in scratch repo, attached from browser, trust dialog answered via keyboard, READY received, cancel finalized run as killed.
- [x] 3r. Phase 3 adversarial review — 5 required (F1 usage dedupe, F2 cancel→resolveParent, F3 double-spawn guard, F4 safeParse, F5 spawn-fail→failed) + 8 recommended; all applied incl. F6 origin guard on non-GET + token on /ws/events, F7 reschedule bit, F8 session caps/GC, F9 pid verify, F10 resize guard, F11 keyword \b, F12 token redaction, F13 boot catch
- [x] 4. Completion detection: internal hook routes (stop/session-end/needs-attention, token-guarded), transcript stats (deduped costs, ctx %), markIdle frees the concurrency slot while the PTY stays attachable, resultSummary from last assistant message (1.5s flush delay + exit backstop). Verified live: worker created/edited files in scratch repo, running→review in 10–15s, summary + stats recorded, kill finalizes runs.
- [x] 4r. Phase 4 adversarial review — 4 required (R1 stats dedupe, R2 idle-session eviction/TTL, R3 stale-Stop hijack + enqueue-from-review guard, R4 hasLiveSession over all runs) + M1-M8; all applied. Plus user-reported fix: completed-idle sessions no longer badge "needs attention" (idle run flag, migration 3) and Queue separates working sessions from idle terminals.
- [x] 5. Orchestrator: claim loop verified live (3 queued children, 2-at-a-time, all → review in 90s), boot recovery (strict claude pid match, recent-ended sweep, review-safe conditional transitions) verified across restart, complete-route parent resolution (children done → parent review → done).
- [x] 6. Analyze + proposals: envelope spike captured (`structured_output` field), headless runner (dontAsk + disallowed write tools, 64MiB buffer), proposals stored/broadcast; accept-split verified live (3 auto children queued + parent blocked); analysis produced grounded proposals from real repo inspection in 30s.
- [x] 5r/6r. Combined adversarial review — 4 required (R1 analyze TDZ/EPIPE crash paths, R2 boot-sweep exitCode==null guard vs pid reuse, R3 conditional split-block before child insert in BOTH drivers, R4 killable analyze runs + hidden Terminal button) + M1-M4 minors; all applied
- [x] 7. Postgres driver: full implementation (pool, ?→$n rewrite, BEGIN/COMMIT composites, FOR UPDATE on accept, Supabase-compatible). Compiles; NOT yet tested against a live connection string — needs a Supabase/Postgres URL in data/config.json to verify.
- [x] 8. Polish (rolling): model/effort overrides in create form + slide-over, idle-terminal section, usage pill, real-time everywhere. Remaining nice-to-haves: drag/column board view, Sentry "create from ref" form.
- [x] Final review of the fix batch + postgres.ts + recent UI — PASS for daily SQLite use; pg pool error-listener (F1, required for postgres), analyze SIGKILL escalation (F3), one-analyze-per-repo (F4), router-aware analyze model (F5) all applied
- [x] Post-launch round (2026-08-24 afternoon): Stop agent button + auto-close terminals on Mark done; Sentry sync (org/project/token config, idempotent issue→task import, EU api base; token needs event:read+project:read — the sourcemap token 403s); Handbook (docs/handbook.md + in-app /handbook page + published artifact); neko-nest CLAUDE.md split under 40k (→ docs/claude-facts.md)
- [x] Agent Task API + cross-repo coordination — design adversarially reviewed FIRST (approved w/ R1–R11), implemented with all 11 changes, live-verified (worker filed a draft via the API; provenance chips on Board). Role model split applied (worker opus / analysis+orchestrator fable). Migration 4 (created_by_run, spawn_depth, run_token).
- [x] Agent API implementation adversarial review — pass with F1 (legacy master-token branch deleted from internal hooks; per-run tokens are the ONLY identity), F2 (null-safe authRun), F3 (overflow never exceeds the PTY hard cap; cap-reached spawns requeue instead of failing); all applied
- [x] Dashboard/observability — design adversarially reviewed FIRST (approved w/ 12 changes, all implemented): tm_events audit log written in-transaction with a REQUIRED actor param (storage-level, compile-enforced), time-sortable ids, migration 5; /api/stats/overview + /api/stats/anomalies (JS day-bucketing on local time, workedMs to first-idle) + /api/events; Dashboard page (tiles, validated 2-series stacked bars + cost bars, anomalies panel, depth/actor panel, per-repo table, live activity feed via event.appended). Chart palette validated with the dataviz six-checks script in both themes. Verified live in browser.
- [x] Dashboard implementation adversarial review — amendment scorecard 12/12 faithful; 4 required fixes applied (R1 anomaly knobs added to settings schema, R2 context/cost anomalies gated to live-or-24h runs, R3 dashboard live-refetch keyed on newest event id not capped length, R4 event broadcasts buffered per-transaction and fired only post-commit in BOTH drivers) + W1 local-midnight window anchor, W2 per-kind event queries vs truncation, W3 proposal rejections now traced. Verified live after restart.

**All work to date is built, adversarially reviewed, and fix-verified.** Outstanding (needs the user): Sentry token with event:read+project:read; Postgres live smoke-test with a real connection string; subscription usage reset before running workers.

## Open-source release prep (2026-08-25)

- [x] Root docs: `README.md` (what it is, requirements, install, first run, storage, security, layout), `LICENSE` (MIT), `CONTRIBUTING.md` (setup, required checks, the conventions that trip people up), `SECURITY.md` (threat model + reporting).
- [x] `package.json` metadata: description, license, repository/bugs/homepage, keywords, `engines.node >=22.12.0`, `typecheck` script; lockfile synced.
- [x] `@fastify/static` 8 → 10.1.3, clearing four high-severity advisories; `npm audit` now reports 0 vulnerabilities.
- [x] `.gitignore` extended (`.env*`, `.claude/settings.local.json`; `server/data/` comment explains it holds credentials and artifacts).
- [x] GitHub Actions CI (`.github/workflows/ci.yml`): `npm ci` + typecheck + build on Node 22 and 24.
- [x] Personal absolute path removed from `docs/design.md`; repo scanned for secrets and emails (none found).
- [x] Header: the reload-icon restart control is now a labelled "Restart server" ghost button (shows "Restarting…" while in flight), sitting on the same row/height as the theme toggle; the unused `IconRestart` glyph was dropped from `Icons.tsx`.
- [x] Header usage: session / weekly / weekly-fable, from the account (2026-08-25). `/api/usage` returns a `UsageSnapshot` whose windows carry `{pct, source, resetsAt, tokens, budget}` — real plan figures read from the CLI's `~/.claude.json` usage cache, local-transcript estimate as the fallback for expired/missing windows. Verified: typecheck + SPA build clean; account reader exercised against the live config (weekly 30% / fable 45% real, expired session correctly falling back) and against a missing config dir; `/api/usage` handler exercised offline incl. a zero-budget guard; estimator cold 549ms / cached 0ms with a shared in-flight scan; both the new pill and its legacy fallback confirmed rendering in the browser. The running server was NOT restarted (two workers active), so the new payload goes live on the user's next restart.

## Row quick actions (2026-08-25)

- [x] Task rows carry their own actions: a terminal glyph **before** the title (attaches to the task's live PTY session, else its most recent run — the same run the slide-over's "Open terminal" would open) and, after the status badge, **run now** (▶) and **mark as ready** (✓ = enqueue for the orchestrator). Extracted as `web/src/components/TaskRow.tsx` and used by both task lists (Board groups, Queue's queued list); Board/Queue now take an `onOpenTerminal` prop from `App`.
- Disabled states mirror the server guards exactly (`run-now`: draft/queued/review/failed/cancelled; `enqueue`: draft/review/failed/cancelled — blocked is deliberately absent; both need a repo), and the reason rides in the tooltip ("Cannot enqueue from 'running'", "Assign a repo before running this task"). Tooltips sit on a wrapper span because `.btn:disabled` sets `pointer-events: none`. Clicks `stopPropagation()` so the row's slide-over does not open; failures render as a transient inline `warn-text` in the row (auto-clears after 6s) instead of an alert.
- Verified: `npm run typecheck` + `npm run build` clean; live in the browser — layout order (terminal · title · chips · badge · actions) with no row overflow at 1080px, disabled matrix correct per status, a scratch task's ✓ moved it draft → queued without opening the slide-over (then deleted), and the terminal glyph on the running row opened the drawer attached to that session ("live"). No console errors.

### Follow-up: ✓ means "mark done" in review, and the live-session guard is visible (2026-08-25)

- User report: clicking ✓ on a task in **review** answered "previous session is still live — open its terminal or kill it first". Two problems behind one click: (a) after a worker's Stop hook the task sits in `review` with its PTY still attachable, so `enqueue` hits the server's `hasLiveSession()` guard — a 409 by design; (b) on a task in review the check should mean what the slide-over's primary button means there, **Mark done**, not "queue it again".
- ✓ is now status-aware: `review` → `POST /tasks/:id/complete` (tooltip "Mark done", `.quick.affirm` tints it `--tm-status-done` on hover — complete also closes the task's idle sessions server-side); every other status keeps `enqueue` ("Mark as ready"). `CAN_READY` drops `review` accordingly.
- ▶ and the enqueue variant of ✓ now also mirror the server's `hasLiveSession()` guard client-side (any run row of the task still `status: 'running'` — the Stop hook sets `idle`, not the status, and boot recovery flips stale rows to `exited`). They disable with that exact sentence as the tooltip instead of letting the click return a 409. The mark-done variant is deliberately exempt: `complete` is legal with a live session.
- Verified: `npm run typecheck` + `npm run build` clean. Live: a scratch task forced to `review` showed ✓ = "Mark done" (`affirm` class) and clicking it moved the task review → done without opening the slide-over and with no row error; with a synthetic live run row attached, ▶ and ✓ read "Previous session is still live — …" and were disabled on `failed`, while on `review` ▶ stayed disabled and ✓ stayed "Mark done"; `running` rows still read "Already running" / "Cannot enqueue from 'running'". Scratch task and synthetic run row deleted afterwards.

## Feature interface (2026-08-25)

- [x] Big request → analysis → adversarial plan review → visual approval → phase-gated execution. Design doc promoted from `docs/future/feature-interface.md` to **`docs/features.md`** with an "As built" section recording the deltas; decision-log entry added; `docs/design.md` structure/data-model/API/orchestrator/frontend sections updated. `docs/future/autonomy-cloud-shadow.md` (the motivating too-big request) now points at the shipped interface.
- Shipped: migration 9 (`tm_features`, `tasks.feature_id/feature_phase`), composite storage methods on BOTH drivers (`createFeature`/`updateFeature`/`transitionFeature`/`deleteFeature`/`approveFeature`/`resolveFeatureCompletion`/`cancelFeature`), the shared phase-gating SQL fragment + its JS twin, the two-headless-run plan pipeline with bounded re-analysis, `/api/features` CRUD + actions, `feature.updated`/`feature.deleted` on `/ws/events`, the read-only `GET /api/agent/features/:id`, the Features list + Feature page (editable phase columns pre-approval, execution dashboard post-approval), a `feature` provenance filter and `feat pN` chips on the Board, and the `feature.analysisMaxRounds` Config row.
- Verified: `npm run typecheck` + `npm run build` clean; a 50-assertion offline suite over the real routes and both composites on a throwaway DB (approval, exclusions, caps injection, phase gating, failure→pause, resume guard, roll-up, cancel, repo detach) — ALL PASS; the bounded re-analysis loop driven deterministically by a stub `claude` (blocker → re-analysis with findings folded in → clean; `maxRounds: 0` stops at one round and still surfaces the blocker); one LIVE pipeline run (93s, fable plan + fable plan review, verdict `minor`, tasks created with caps present); and the whole UI flow in the browser with no console errors.
- Verification ran on a **separate scratch server instance** (own port + throwaway DB, config restored immediately); the user's running server was never restarted, so the new API goes live on their next restart. The rebuilt SPA is already in `web/dist` — until that restart, its Features page will find no `/api/features` route and simply show nothing.

## Agent task-filing cap is configurable (2026-08-25)

- [x] The per-run agent task-creation cap moved from a hardcoded `5` in `server/src/routes/agent.ts` to the settings key **`agent.taskCreationCap`** (default **15**, range 1–100, zod-validated in `PUT /api/config`, re-clamped at read time so a hand-edited `tm_config` row cannot disable the guard or set it out of range; a non-numeric/null row falls back to the default).
- `GET /api/agent/context` now also returns `taskCreationCap` / `tasksCreated` / `tasksRemaining`, and `GET /api/agent/instructions` interpolates the live value into `{{taskCreationCap}}` in `docs/agent-instructions.md` so the served sheet can never drift from the setting. Config gained a **Tasks an agent may file** row; handbook Config reference + `docs/agent-api-design.md` + decision log updated.
- The global queued-agent ceiling (10) stays hardcoded and independent — past it, honored `enqueue:true` degrades to a draft rather than erroring, so a larger per-run cap widens what an agent may *file*, never what it can auto-start.
- Verified: `npm run typecheck` + `npm run build` clean; a 21-assertion offline suite over the real routes on a throwaway SQLite DB (`server/data/artifacts/<task>/agent-cap-test.ts`) — default 15 honored (15 filings pass, 16th 403s naming the cap), raising the cap via `PUT /api/config` resumes filing, lowering it below the already-filed count refuses cleanly with `tasksRemaining` clamped at 0, PUT rejects 0/101/1.5/-3, raw DB values 0 / 999999 / `"abc"` / `null` / `7.9` resolve to 1 / 100 / 15 / 15 / 7, instructions text follows the current cap, and the depth cap still fires independently — ALL PASS. The user's running server was not restarted; the change goes live on their next restart.

## Board density: recent strip, capped drafts, collapsed done (2026-08-25)

- [x] Three trims to `web/src/pages/Board.tsx`, all respecting the active repo/source/category filters.
- **recent** — a new top section listing the **10 most recently updated** tasks whatever their status (`updatedAt` desc, `id` as tiebreaker since ISO timestamps collide on same-transaction writes). It intentionally duplicates rows that also appear in the groups below; it is a shortcut, not a partition. Hidden when the filtered set is empty.
- **draft** — sorted newest-first (the rest of the board keeps the server's `priority DESC, created_at` order; sorting is what makes "last 7" mean anything) and capped at **7** rows behind a `show all N` / `show less` toggle, which only appears once there are more than 7.
- **done** — renders header-only until `show all N` is clicked; its `.panel` is skipped entirely while collapsed so no empty bordered box is left behind.
- Both toggles are gated on `groupBy === 'status'`, so a category or repo literally named "draft"/"done" is never trimmed. State is component-local and not persisted (a remembered "done is hidden" is easy to forget you set).
- The per-row chip/badge block was extracted into a local `row(task, ctx)` helper shared by the recent strip and the groups (`ctx` replaces the old `groupBy !==` checks that suppress the redundant category/repo chip). New `.section-head .section-toggle` rule in `web/src/theme.css` gives the button the header's mono micro-type; it resolves entirely to `--tm-*` tokens.
- Verified: `npm run typecheck` + `npm run build` clean; then in the browser against the running server (NOT restarted — only `web/dist` was rebuilt, which it serves from disk): recent showed 10 mixed running/review/done/draft rows and live-reordered over `/ws/events` when a task's `updatedAt` changed; draft read `20 · show all 20` with exactly 7 newest-first rows (T10…T5) and expanded to all 20 on click; done read `26 · show all 26` with no panel and expanded to the full list, both buttons flipping to `show less`. No console errors.

## Board: "active" section on top (2026-08-25)

- [x] `web/src/pages/Board.tsx` — a new **active** section renders above `recent`, listing exactly the `running` and `review` tasks of the current filter: `ACTIVE.flatMap(s => withChildren(filtered.filter(status === s).sort(byRecency('updatedAt'))))`, so running rows come before review rows, newest touch first inside each, children under their parent. No cap and no toggle — active work is what must never be hidden.
- Under `group: status` the `running` and `review` groups are skipped (the strip is those two groups; showing both would duplicate the same rows two sections apart). Under `group: category`/`repo` nothing is skipped — the strip is a cross-cutting pin like `recent`, so a running task appears both on top and inside its repo/category group.
- Repo/source/category filters apply (the section is built from `filtered`); the section disappears when nothing is running or in review. The row helper's `ctx` gained `'active'`, which suppresses no chip — the strip mixes repos and categories, so both chips are wanted there.
- `web/src/theme.css`: new `.section-head.accent` (+ its `.count`) paints the header with `--tm-accent`; no new spacing, borders or hardcoded values — the rule resolves entirely to `--tm-*` tokens.
- Verified: `npm run typecheck` + `npm run build` clean; then in the browser against the running server (NOT restarted — only `web/dist` was rebuilt, which it serves from disk). With 2 running + 1 review task the board opened on `ACTIVE 3` in accent teal above `RECENT 10`, and the groups below started at `draft` — no `running`/`review` group in sight. Switching to `group: repo` kept the same 3-row active strip on top AND showed the running rows inside their repo groups. No console errors.

## Proceed: continue a dead agent's session + configurable terminal keep-alive (2026-08-25)

- [x] **Resume plumbing** — `buildWorkerInvocation` takes `resumeSessionId`: args become `claude --resume <id> --model … --effort … --settings <hooks> <prompt>`, and the prompt drops the task restatement/standing-rules block for a short "continuing task" header + the new instruction + a one-paragraph re-anchor of the caps (in case the session was compacted). `DEFAULT_PROCEED` is what an empty Proceed sends: carry on, and re-check the state of the work before assuming anything.
- [x] **Orchestrator** — `findResumableRun()` (newest worker run of the task with a `session_id`, same repo, transcript still on disk), `waitForSessionExit()`, `followUp(…, mode: 'auto'|'resume'|'fresh')` and `proceed()`. `startWorker(task, followUp, resumeFrom)` snapshots the inherited transcript totals into the new run's `stats_baseline` and passes `resumeSessionId` through. `resumableSessionId()` backs the UI.
- [x] **No double-billing** — `claude/stats.ts` gained `netStats(raw, baseline)` + `summarizeRun(run, tp?)`; the three places that wrote run stats from a transcript (live-stats tick, exit finalizer, hook `recordRunInfo`) now go through it. `contextPct` is passed through, not subtracted.
- [x] **Two exit-path fixes** the resume flow exposed: the PTY kill in `followUp` is now awaited (≤5s) for every respawn — previously the immediately-following `startWorker` could see the dying PTY and refuse with "previous session is still live" (reproduced as a 500) — and `handleExit` no longer drives a task's status for a run that was deliberately `killed` or superseded by a newer run of the same task.
- [x] **Schema** — migration 10 adds `tm_runs.resumed_from` + `tm_runs.stats_baseline` (both drivers' `createRun`, `rowToRun`, `Run`/`NewRun` types). `updateRun`'s statement doesn't touch either column, so they are write-once at spawn.
- [x] **Config** — `agent.resumeSessions` (default on: follow-ups continue the previous session) and `pty.sessionTtlMinutes` (default 30, `0` = keep terminals forever), both zod-validated and exposed as Config rows. `server/src/index.ts` re-reads scrollback + TTL every 30s so neither needs a restart; `SessionManager` takes a `ttlMs()` accessor and skips age-based GC entirely when it is ≤ 0.
- [x] **API/UI** — `POST /api/tasks/:id/proceed {message?}` and `GET /api/tasks/:id/resumable`; the task panel's follow-up block gained a **Proceed** button (+ a `resumes <id8>` hint and a disabled-state explanation), and its placeholder now says a follow-up continues the previous session. Both buttons share one busy/error handler.
- Verified end-to-end against an isolated second instance (port 5176, its own SQLite DB and artifacts dir — the user's server on 5175 was never touched or restarted): `npm run typecheck` + `npm run build` clean; migration 10 applied to a fresh DB.
  - **Continuity, dead terminal**: task told the agent to remember `4711`; after it finished, *Stop agent* killed the PTY, then `POST /proceed` with "write the number you were asked to remember" → new run with `resumedFrom` = the old run, the SAME `session_id`, and `answer.txt` containing **4711**.
  - **Continuity, live idle session**: second task with codeword `ZEPHYR-93`; a follow-up sent while the idle session was still alive returned 200 (the pre-fix race returned 500), resumed the same session, and wrote **ZEPHYR-93**.
  - **No double-billing**: baseline `$0.063` inherited, resumed run reported `$0.036` (raw transcript total `$0.099`), and its `run.stats-final` event carried the delta, not the total.
  - **Refusals**: proceed on a never-run task → 409 "no resumable agent session…", `resumable` → `{resumable:false}`, empty POST body handled; `pty.sessionTtlMinutes` accepts 0 and rejects -1.
  - **`agent.resumeSessions:false`** → follow-up logged `delivery: respawn`, `resumedFrom: null`, and ran a fresh session.
  - **TTL semantics** (direct `SessionManager` test): with `ttl=0` an idle session backdated 10h survived a GC tick; the `MAX_LIVE_SESSIONS` pressure eviction still capped live PTYs at 10 with that infinite TTL; with `ttl=60s` the aged-out idle session was evicted while a freshly-idle one was kept.
  - `claude --resume <unknown-uuid>` prints "No conversation found with session ID" and exits — which is exactly why resumability requires the transcript to still be on disk.


## Board: drafts ↔ recent swap, ages, sort, folding, essentials mode (2026-08-25)

- [x] **Swapped the strips** — `web/src/pages/Board.tsx` renders **active → drafts → groups → recent**. Drafts left the grouped lists and became a first-class strip right under active (still capped at `DRAFT_LIMIT` = 7 with `show all N`); `recent` moved to the bottom of the page. Under `group: status` both `draft` and the two ACTIVE statuses are skipped in the grouped section; under `group: category`/`repo` they stay pins, exactly like the active strip already behaved.
- [x] **New vs old** — new `web/src/components/TimeAgo.tsx`: one module-level 30s clock shared by every row (`useNow`), `fmtAgo` ("now/9m/5h/3d/6w/2y", negative ages from clock skew clamp to "now"), and a `TimeAgo` span whose tooltip names which timestamp it shows plus both absolute times. A task filed under 24h ago (`isNew`) gets `.task-row.fresh` (inset accent edge — no layout shift) and an accent age; past 14 days the age dims (`.age.stale`). Which timestamp a row shows: `createdAt` in drafts (a draft never ran), `updatedAt` in recent, otherwise the field the active sort uses.
- [x] **Sorting** — a 5th select: last touched (default) / newest filed / oldest filed / title A–Z. `comparator()` breaks every tie on `id` (ISO timestamps collide on same-transaction writes) and `ordered()` applies it to active, drafts and every group before `withChildren` re-nests children. `recent` deliberately ignores it — it *is* the last-touched list.
- [x] **Hiding** — every section header is now a `Section` fold button (`.section-fold`, `aria-expanded`, rotating `IconChevron`); a folded section renders no panel. `status:done` and `status:cancelled` start folded. Fold keys are `${groupBy}:${label}`, so a category literally named "done" folds independently of the status bucket.
- [x] **Essentials mode** — header toggle (`aria-pressed`): rows drop to title + status badge, and `recent` + done/cancelled disappear. Sort, essentials and the folded set persist in `localStorage['tm.board']`, parsed defensively (bad JSON, blocked storage or an unknown sort key falls back to defaults); the repo/source/category/group filters stay per-session as before.
- [x] `web/src/components/TaskRow.tsx` gained an optional `fresh` prop (the accent edge); `web/src/components/Icons.tsx` gained `IconChevron`. New `.board-bar` (wrapping filter/sort toolbar, so five selects no longer crowd the page title), `.section-fold`/`.caret`, `.age`/`.age.fresh`/`.age.stale`, `.task-row.fresh` in `web/src/theme.css` — all `--tm-*` tokens, no hardcoded values.
- Verified: `npm run typecheck` + `npm run build` clean; then in the browser against the running server (NOT restarted — only `web/dist` was rebuilt, which it serves from disk). Board opened `ACTIVE 3 → DRAFTS 31 (show all 31, 7 rows) → DONE 34 (folded) → RECENT 10` with ages 5m…13m and accent edges on every row. Folding DONE expanded 34 rows (ages 10m…6h). Switching sort to *oldest filed* reordered active and drafts and surfaced the `1d` drafts — which correctly render with **no** accent edge while the `11h` ones keep it. *essentials* dropped every chip/age and hid recent + done, and the button flipped to *full view*. `localStorage['tm.board']` round-tripped sort/fold/essentials across a reload. Checked in both themes; no console errors.
